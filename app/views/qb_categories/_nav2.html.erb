<%= tab 'building', 'Customers', {controller: :qb_customers, context: controller.singularize, context_id: obj.id}, action: :index %>
<%= tab 'money', 'Charges', {controller: :qb_charges, context: controller.singularize, context_id: obj.id}, action: :index %>